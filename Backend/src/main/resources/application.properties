spring.application.name=Backend

# ========== 本地MySQL数据库配置 ==========
spring.datasource.url=jdbc:mysql://localhost:3306/laiou_backend?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=00077
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ========== JPA/Hibernate 配置 ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ========== 连接池配置 ==========
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.leak-detection-threshold=60000

# ========== Jackson配置 ==========
# 处理Hibernate懒加载代理对象
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null
# 启用 Java 8 时间类型支持
spring.jackson.deserialization.adjust-dates-to-context-time-zone=false

# ========== JWT ==========
# 生成一个足够长的 Base64 密钥（示例，仅开发环境）
app.jwt.secret=dGhpc0lzQVN1ZmZpY2llbnRMb25nU2VjcmV0S2V5Zm9ySmF2YVNwcmluZw==
# 访问令牌有效期（毫秒）：30分钟
app.jwt.access-ttl-ms=1800000
